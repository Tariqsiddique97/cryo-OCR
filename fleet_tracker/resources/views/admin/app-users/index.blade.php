<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Manage App Users') }}</h2></x-slot>
    <div class="py-12"><div class="max-w-7xl mx-auto sm:px-6 lg:px-8"><div class="bg-white overflow-hidden shadow-sm sm:rounded-lg"><div class="p-6 bg-white border-b"><div class="flex justify-end mb-4"><a href="{{ route('admin.app-users.create') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Create New User</a></div>
    <table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Name</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Contact</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Role</th><th class="relative px-6 py-3"></th></tr></thead>
    <tbody class="bg-white divide-y">@forelse ($users as $user)<tr><td class="px-6 py-4"><div class="font-medium">{{ $user->name }}</div><div class="text-sm text-gray-500">{{ $user->username }}</div></td><td class="px-6 py-4 text-sm text-gray-500"><div>{{ $user->email }}</div><div>{{ $user->country_code }} {{ $user->phone_number }}</div></td><td class="px-6 py-4 text-sm">{{ $user->appRole->name ?? 'N/A' }}</td><td class="px-6 py-4 text-right text-sm font-medium"><a href="{{ route('admin.app-users.show', $user->id) }}" class="text-gray-600">View</a><a href="{{ route('admin.app-users.edit', $user->id) }}" class="text-indigo-600 ml-4">Edit</a><form class="inline-block" action="{{ route('admin.app-users.destroy', $user->id) }}" method="POST" onsubmit="return confirm('Are you sure?');">@csrf @method('DELETE')<button type="submit" class="text-red-600 ml-4">Delete</button></form></td></tr>@empty<tr><td class="px-6 py-4 text-center" colspan="4">No users found.</td></tr>@endforelse</tbody></table>
    </div></div></div></div>
</x-app-layout>